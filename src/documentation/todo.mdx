TODO:
add robots.txt with content:

"'User-agent: \*'"
"'Disallow: /'"

add sitemap.xml with content

add cms :https://decapcms.org/docs/add-to-your-site/

fix the logo and title spacing

fix the grid (magic grid)

fix section on mdx files in blog

turn the articles into cards

create seperate page/overlay for the images through an onClick event

optimize the footer

put an H1 into its own component and fix the menu

add an H1 to every page
